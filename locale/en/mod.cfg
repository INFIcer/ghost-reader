[mod-name]
ghost-reader=Ghost Reader

[mod-description]
ghost-reader=In vanilla Factorio, the tasks of construction robots cannot be read as circuit signals, yet this is crucial for automating construction. This mod focuses on solving that. It adds a new entity that reads the ghost requests within the whole surface or its logistics network and outputs them as circuit signals, fully configurable via a GUI. It supports adjusting the filter mode (entities | tiles | upgrades | items) and the quantity mode (supply requests | recycling requests).

[technology-name]
ghost-reader=Ghost Reader

[entity-name]
ghost-reader=Ghost Reader

[item-name]
ghost-reader=Ghost Reader

[technology-description]
ghost-reader=It can read the ghosts within the whole surface or the construction area of its logistics network.

[item-description]
ghost-reader=It can read the ghosts within the whole surface or the construction area of its logistics network.

[gr-gui]
title=Ghost Reader
range-mode=Scan range:
mode-surface=Surface
mode-network=Logistics network
current-range=Current range:
filter=Filter:
filter-all=All
filter-entity=Entities
filter-tiles=Tiles
filter-upgrades=Upgrades
filter-items=Items
qty=Quantity mode:
qty-net=Supply - Recycling
qty-supply=Supply only
qty-recycle=Recycling only
output=Current output signals
network-prefix=Network #
status-no-network=Not inside a logistics network
close=Close
warn-prefix=[Ghost Reader Warning]
warn-region-rebuild=Region full-rebuild fallback was triggered (mark_meta missing) so a full rescan was used instead of incremental rollback. This should NOT happen in normal operation; please report if it repeats.

[gr-tooltip]
range-mode=Scan range
current-range=Current range
filter=Filter
qty=Quantity mode
